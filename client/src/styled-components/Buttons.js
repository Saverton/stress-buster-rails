import styled from 'styled-components';

const Button = styled.button`
  padding: 0.5rem 1rem;
  margin: .5em 0em 2em 0em;
  border-radius: 40px;
  border: none;
  font-size: large;
  font-weight: bold;
`;

export { Button };
